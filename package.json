{
  "name": "refeed-workflows",
  "private": true,
  "type": "module",
  "description": "Workflow-only RSS rule management and feed publishing",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "fast-xml-parser": "^5.3.0"
  }
}
